# Expense Tracker (JS)

A simple JavaScript project that tracks personal expenses, flags high-spending
categories, calculates total spending, and checks that spending against a
monthly income budget. All output is logged to the browser console.

## Project Structure
